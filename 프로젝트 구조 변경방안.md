# 🏗️ 프로젝트 구조 리팩토링 계획

## 📋 TL;DR (핵심 요약)

**목표:** `Client(lib) + Editor(exe) + Game(exe) + Engine(dll)` 구조로 리팩토링

**변경사항:**
- ✅ Client: `exe` → `lib` (게임 로직 공유)
- ✅ Game: 신규 생성 (네트워크 포함 게임 클라이언트)
- ✅ Editor: `EditorApp` → `Editor` (이름 변경)
- ✅ NetworkManager: `Client` → `Game`으로 이동

**기대효과:**
- 코드 재사용 (Client.lib를 Editor/Game이 공유)
- 명확한 의존성 (순환 참조 제거)
- 관심사 분리 (게임 로직 vs 네트워크 vs 에디터)

---

## 📚 목차

- [Context (현재 문제점)](#context)
- [아키텍처 설계 결정사항](#아키텍처-설계-결정사항)
  - [EditorTool 처리 방안](#1-editortool-처리-방안)
  - [NetworkManager 처리 방안](#2-networkmanager-처리-방안)
- [최종 프로젝트 구조](#최종-프로젝트-구조)
- [상세 구현 계획](#상세-구현-계획)
  - [Phase 1: Client를 StaticLibrary로 변환](#phase-1-client를-staticlibrary로-변환)
  - [Phase 2: Game.exe 프로젝트 생성](#phase-2-gameexe-프로젝트-생성)
  - [Phase 3: EditorApp를 Editor로 이름 변경](#phase-3-editorapp를-editor로-이름-변경)
  - [Phase 4: 솔루션 파일 업데이트](#phase-4-솔루션-파일-업데이트)
  - [Phase 5: 네트워크 코드 이동](#phase-5-네트워크-코드-이동)
  - [Phase 6: 빌드 설정 정리](#phase-6-빌드-설정-정리)
  - [Phase 7: 코드 정리](#phase-7-코드-정리)
- [검증 계획](#검증-계획)
- [실행 매뉴얼 (A to Z)](#-실행-매뉴얼-a-to-z)
- [리팩토링 이점](#리팩토링-이점)

---

<a name="context"></a>
## 🔍 Context (현재 문제점)

현재 프로젝트는 `EditorApp(exe)`, `EditorTool(lib)`, `Client(exe)`, `Engine(dll)` 구조로 되어 있으나, 다음과 같은 문제가 발생:

1. **EditorApp이 Client의 include path를 참조** - 실제 코드는 주석 처리되었지만 vcxproj에 남아있음
2. **MainApp 초기화 로직 중복** - Client/MainApp.cpp와 EditorApp/Editor_MainApp.cpp의 GameInstance 초기화가 유사
3. **의존성 불일치** - Debug/Release 설정이 다르고, Client → EditorTool 의존성 제거 진행 중
4. **공통 게임 로직 재사용 불가** - Level, GameObject 등을 Editor와 Game이 독립적으로 관리

**목표:** `Client(lib)`, `Editor(exe)`, `Game(exe)`, `Engine(dll)` 구조로 리팩토링하여 게임 로직을 공유하고 의존성을 명확히 함.

---

<a name="아키텍처-설계-결정사항"></a>

## ⚙️ 아키텍처 설계 결정사항

<a name="1-editortool-처리-방안"></a>
### 1️⃣ EditorTool 처리 방안

**결정: EditorTool.lib를 Editor.exe에 병합하지 않고 유지**

**이유:**
- EditorTool은 5,395줄의 잘 구조화된 코드 (23개 클래스, 8개 뷰)
- ImGui_Manager, Editor_Manager, PlayerSession_Manager 등이 독립적으로 동작
- PlayerSession_Manager는 외부 프로세스 관리 (Client.exe×N 실행)에 특화
- EditorInstance를 통한 명확한 인터페이스 제공
- 게임 로직(Client.lib)과 에디터 UI(EditorTool.lib)의 관심사 분리

**최종 구조:**
```
Editor.exe → EditorTool.lib → Engine.dll
          → Client.lib → Engine.dll
Game.exe → Client.lib → Engine.dll
```

<a name="2-networkmanager-처리-방안"></a>
### 2️⃣ NetworkManager 처리 방안

**결정: NetworkManager를 Game.exe에만 포함 (Client.lib에 넣지 않음)**

**이유:**
- NetworkManager는 ServerCore.lib에 의존 (ClientService, IocpCore 사용)
- Client.lib에 넣으면 Editor.exe도 불필요하게 ServerCore.lib 링크 필요
- lib 수준에서 SocketUtils, IocpCore 같은 시스템 리소스 초기화 부적절
- Editor는 별도 프로세스(Client.exe×N)를 실행하는 방식으로 멀티플레이 테스트 (네트워크 직접 사용 불필요)
- 라이브러리 순수성 유지 (Client.lib = 게임 로직만, 네트워크 제외)

**구체적 배치:**
```
Game.exe
├── MainApp.cpp (Application layer)
│   ├── NetworkManager::Initialize()
│   └── NetworkManager::Update()
├── NetworkManager (exe 레벨 포함)
├── Client.lib (게임 로직)
└── ServerCore.lib (정적 링크)

Editor.exe
├── Editor_MainApp.cpp
├── EditorTool.lib
│   └── PlayerSession_Manager (Game.exe 프로세스 실행)
├── Client.lib (게임 로직)
└── ServerCore.lib 의존성 없음
```

---

<a name="최종-프로젝트-구조"></a>
## 🎯 최종 프로젝트 구조

### 📊 의존성 다이어그램

```
Engine.dll (DynamicLibrary)
   ↑
   ├─ Client.lib (StaticLibrary)
   │    ├── 게임 로직 (Level, GameObject, Component 등)
   │    ├── ResourceLoader
   │    └── 공통 초기화 코드
   │
   ├─ EditorTool.lib (StaticLibrary)
   │    ├── EditorInstance (에디터 최상위 제어)
   │    ├── ImGui_Manager
   │    ├── Editor_Manager (뷰 관리)
   │    ├── PlayerSession_Manager (Client.exe 프로세스 실행)
   │    └── 8개 EditorWindow (Scene, Game, Hierarchy 등)
   │
   ├─ Editor.exe (Application)
   │    ├── depends: EditorTool.lib
   │    ├── depends: Client.lib
   │    └── Editor_MainApp.cpp
   │
   └─ Game.exe (Application)
        ├── depends: Client.lib
        ├── depends: ServerCore.lib
        ├── MainApp.cpp
        └── NetworkManager (exe 레벨)
```

### 🔄 프로젝트 이름 매핑

| 기존 | 변경 후 | 타입 |
|------|---------|------|
| Client.vcxproj | Client.vcxproj | StaticLibrary (변경) |
| EditorApp.vcxproj | Editor.vcxproj | Application (이름 변경) |
| - | Game.vcxproj | Application (신규) |
| EditorTool.vcxproj | EditorTool.vcxproj | StaticLibrary (유지) |
| Engine.vcxproj | Engine.vcxproj | DynamicLibrary (유지) |

---

<a name="상세-구현-계획"></a>
## 📝 상세 구현 계획

<a name="phase-1-client를-staticlibrary로-변환"></a>
### 🔷 Phase 1: Client를 StaticLibrary로 변환

**파일:** `Client/Default/Client.vcxproj`

**변경사항:**
1. `<ConfigurationType>Application</ConfigurationType>` → `<ConfigurationType>StaticLibrary</ConfigurationType>`
2. WinMain 진입점 제거 (Game.exe로 이동)
3. MainApp.cpp에서 NetworkManager 참조 제거 (Game.exe로 이동)
4. AdditionalDependencies에서 ServerCore.lib 제거
5. AdditionalIncludeDirectories에서 `$(SolutionDir)Server\ServerCore\Public`, `$(SolutionDir)Server\Protobuf\Bin` 제거
6. 출력 파일: `Client.lib`

**이동할 파일:**
- `Client/Private/MainApp.cpp` → `Game/Private/MainApp.cpp` (NetworkManager 포함 버전)
- `Client/Public/MainApp.h` → `Game/Public/MainApp.h`
- `Client/Default/main.cpp` (WinMain) → `Game/Default/main.cpp`

**Client.lib에 남을 파일:**
- Level 구현체 (Level_Loading, Level_Gameplay 등)
- GameObject, Component 구현체
- ResourceLoader
- 공통 게임 로직

<a name="phase-2-gameexe-프로젝트-생성"></a>
### 🔷 Phase 2: Game.exe 프로젝트 생성

**새 프로젝트:** `Game/Default/Game.vcxproj`

**설정:**
- ConfigurationType: Application
- 출력 파일: `Game.exe`

**Dependencies:**
```xml
<AdditionalIncludeDirectories>
  ../Public/;
  ../../EngineSDK/Include/;
  ../Default/;
  $(SolutionDir)Client\Public;
  $(SolutionDir)Client\Default;
  $(SolutionDir)Server\ServerCore\Public;
  $(SolutionDir)Server\Protobuf\Bin;
  %(AdditionalIncludeDirectories)
</AdditionalIncludeDirectories>

<AdditionalDependencies>
  libprotobuf.lib;
  Engine.lib;
  Client.lib;
  ServerCore.lib;
  %(AdditionalDependencies)
</AdditionalDependencies>
```

**포함할 파일:**
- `main.cpp` (WinMain 진입점)
- `MainApp.h/cpp` (NetworkManager 초기화 포함)
- `NetworkManager.h/cpp` (Client에서 이동)
- `ServerSession.h/cpp`
- `Client_PacketHandler.h/cpp`
- `Client_Defines.h`, `pch.h/cpp`

**폴더 구조:**
```
Game/
├── Public/
│   ├── MainApp.h
│   ├── NetworkManager.h
│   ├── ServerSession.h
│   └── Client_PacketHandler.h
├── Private/
│   ├── MainApp.cpp
│   ├── NetworkManager.cpp
│   ├── ServerSession.cpp
│   └── Client_PacketHandler.cpp
└── Default/
    ├── main.cpp
    ├── pch.h/cpp
    ├── Client_Defines.h
    └── Game.vcxproj
```

<a name="phase-3-editorapp를-editor로-이름-변경"></a>
### 🔷 Phase 3: EditorApp를 Editor로 이름 변경

**파일 이동:**
- `EditorApp/` → `Editor/`
- `EditorApp.vcxproj` → `Editor.vcxproj`
- `EditorApp_Defines.h` → `Editor_Defines.h`
- `Editor_MainApp.h/cpp` (이름 유지, 네임스페이스 체크)

**vcxproj 수정:**
```xml
<AdditionalIncludeDirectories>
  ../Public/;
  ../../EngineSDK/Include/;
  ../Default/;
  $(SolutionDir)EditorTool\Public;
  $(SolutionDir)Client\Public;        <!-- 추가 -->
  $(SolutionDir)Client\Default;       <!-- 추가 -->
  $(SolutionDir)Server\ServerCore\Public;  <!-- 제거 -->
  $(SolutionDir)Server\Protobuf\Bin;       <!-- 제거 -->
  %(AdditionalIncludeDirectories)
</AdditionalIncludeDirectories>

<AdditionalDependencies>
  Engine.lib;
  EditorTool.lib;
  Client.lib;         <!-- 추가 -->
  <!-- ServerCore.lib 제거 -->
  %(AdditionalDependencies)
</AdditionalDependencies>
```

**주석 처리 제거:**
```cpp
// Editor/Public/Editor_Defines.h
// 기존: //#include "Client_Defines.h"
#include "Client_Defines.h"  // Client.lib 사용하므로 활성화
```

<a name="phase-4-솔루션-파일-업데이트"></a>
### 🔷 Phase 4: 솔루션 파일 업데이트

**파일:** `Dx11_Naruto.sln`

**프로젝트 참조 순서:**
1. Engine.vcxproj (DLL)
2. Client.vcxproj (LIB) → Engine 의존
3. EditorTool.vcxproj (LIB) → Engine 의존
4. Editor.vcxproj (EXE) → EditorTool, Client 의존
5. Game.vcxproj (EXE) → Client, ServerCore 의존

**빌드 순서 설정:**
```
Engine → Client, EditorTool → Editor, Game
```

### Phase 5: 네트워크 코드 이동

**Client → Game 이동:**

| 파일 | 기존 경로 | 새 경로 |
|------|----------|---------|
| NetworkManager.h | Client/Public/ | Game/Public/ |
| NetworkManager.cpp | Client/Private/ | Game/Private/ |
| ServerSession.h | Client/Public/ | Game/Public/ |
| ServerSession.cpp | Client/Private/ | Game/Private/ |
| Client_PacketHandler.h | Client/Public/ | Game/Public/ |
| Client_PacketHandler.cpp | Client/Private/ | Game/Private/ |

**Client.lib에서 네트워크 참조 제거:**
- `Client_Defines.h`에서 `#include "NetworkManager.h"` 제거
- 다른 게임 로직 클래스에서 NetworkManager 참조 제거 (있을 경우)

**Game.exe의 MainApp.cpp:**
```cpp
#include "NetworkManager.h"

HRESULT MainApp::Initialize()
{
    // Engine 초기화
    ENGINE_DESC engineDesc = {};
    // ... (기존 코드)
    GAME->Initialize_Engine(engineDesc, _device, _context);

    // Static Level 로드
    CHECK_FAILED(Ready_StaticLevel(), E_FAIL);

    // Start Level 로드
    CHECK_FAILED(Ready_StartLevel(ELevelType::GamePlay), E_FAIL);

    // 네트워크 초기화 (Game.exe 전용)
    NetworkManager::GetInstance()->Initialize();

    return S_OK;
}

void MainApp::Update(float timeDelta)
{
    GAME->Update_Engine(timeDelta);
    NetworkManager::GetInstance()->Update();  // Game.exe 전용
}
```

### Phase 6: 빌드 설정 정리

**모든 프로젝트에서 Debug/Release 설정 통일:**

1. **Client.vcxproj (LIB):**
   - Debug|x64, Release|x64 모두 동일 설정
   - EditorTool 관련 include/lib 완전 제거

2. **Game.vcxproj (EXE):**
   - ServerCore.lib, libprotobuf.lib 링크
   - Client.lib 링크

3. **Editor.vcxproj (EXE):**
   - ServerCore.lib 제거
   - Client.lib, EditorTool.lib 링크

**출력 디렉토리:**
```
Bin/
├── Engine.dll
├── Client.lib (중간 산출물, Bin에 복사 불필요)
├── EditorTool.lib (중간 산출물)
├── Editor.exe
└── Game.exe
```

### Phase 7: 코드 정리

**Editor_MainApp.cpp:**
```cpp
// Editor는 NetworkManager 미사용 (주석 해제 필요 없음)
void Editor_MainApp::Update(float timeDelta)
{
    EDITOR->Update_Editor(timeDelta);

    if (EDITOR->IsPlaying()) {
        GAME->Update_Engine(timeDelta);
    }

    // NetworkManager 사용 안 함 (PlayerSession_Manager가 별도 프로세스 실행)
}
```

**ResourceLoader 이동 확인:**
- 이미 Engine으로 이동되었음 (`Engine/Public/ResourceLoader.h`)
- Client.lib와 Editor.exe 모두 Engine을 통해 접근

## 검증 계획

### 빌드 검증

1. **빌드 순서 확인:**
   ```bash
   # Clean 후 Rebuild All
   Engine.dll → Client.lib → EditorTool.lib → Editor.exe, Game.exe
   ```

2. **링크 에러 체크:**
   - Client.lib: NetworkManager 미정의 에러 없어야 함
   - Editor.exe: Client.lib의 Level, GameObject 접근 가능
   - Game.exe: NetworkManager 정상 링크

3. **의존성 확인:**
   ```bash
   # Visual Studio → Project Dependencies
   Editor → EditorTool, Client, Engine
   Game → Client, ServerCore, Engine
   ```

### 런타임 검증

1. **Editor.exe 실행:**
   - EditorInstance 초기화 성공
   - Client.lib의 Level_Editor 로드 성공
   - Scene View, Game View 렌더링 확인
   - Play 버튼 → PlayerSession_Manager가 Game.exe 실행 확인

2. **Game.exe 실행:**
   - NetworkManager 초기화 성공
   - GameServer.exe 연결 확인
   - Level_Gameplay 로드 성공
   - 게임 루프 정상 동작

3. **멀티플레이 테스트:**
   - Editor에서 "Start Multiplayer (2 Players)" 실행
   - GameServer.exe 1개 + Game.exe 2개 프로세스 확인
   - 각 클라이언트 윈도우 위치 정렬 확인

## Critical Files

**수정할 프로젝트 파일:**
- `Client/Default/Client.vcxproj`
- `EditorApp/Default/EditorApp.vcxproj` → `Editor/Default/Editor.vcxproj`
- `Dx11_Naruto.sln`

**생성할 프로젝트:**
- `Game/Default/Game.vcxproj`
- `Game/Public/*.h`
- `Game/Private/*.cpp`
- `Game/Default/main.cpp`

**이동할 파일:**
- `Client/Public/NetworkManager.h` → `Game/Public/`
- `Client/Private/NetworkManager.cpp` → `Game/Private/`
- `Client/Public/ServerSession.h` → `Game/Public/`
- `Client/Private/ServerSession.cpp` → `Game/Private/`
- `Client/Public/Client_PacketHandler.h` → `Game/Public/`
- `Client/Private/Client_PacketHandler.cpp` → `Game/Private/`
- `Client/Public/MainApp.h` → `Game/Public/`
- `Client/Private/MainApp.cpp` → `Game/Private/`
- `Client/Default/main.cpp` → `Game/Default/`
- `Client/Public/Client_Defines.h` → `Game/Public/`
- `Client/Default/pch.h/cpp` → `Game/Default/`

**수정할 헤더:**
- `Editor/Public/Editor_Defines.h` (Client_Defines.h include 활성화)

## 리팩토링 이점

### 1. 코드 재사용
- Client.lib를 Editor와 Game이 공유
- Level, GameObject, Component 등 중복 제거
- ResourceLoader 같은 공통 초기화 로직 통합

### 2. 명확한 의존성
- lib → exe 방향의 일관된 의존성
- 순환 참조 위험 제거
- 빌드 순서 명확화

### 3. 관심사 분리
- Client.lib: 순수 게임 로직
- EditorTool.lib: 에디터 UI/도구
- Game.exe: 네트워크 + 게임 실행
- Editor.exe: 에디터 + 게임 편집

### 4. 포트폴리오 가치
- 아키텍처 설계 능력 어필
- "라이브러리 분리 및 재사용" 컨셉
- 대규모 프로젝트 구조 경험 증명

## 예상 소요 시간

- Phase 1-2 (Client.lib 변환 + Game.exe 생성): 2-3시간
- Phase 3 (Editor 이름 변경): 30분
- Phase 4 (솔루션 업데이트): 30분
- Phase 5 (네트워크 코드 이동): 1시간
- Phase 6 (빌드 설정 정리): 1시간
- Phase 7 (검증 및 테스트): 1-2시간

**총 예상 시간: 6-8시간**

---

# 🔧 실행 매뉴얼 (A to Z)

> **주의:** 작업 전 반드시 Git 커밋 또는 백업을 완료하세요!

## 📌 사전 준비

### ✅ 체크리스트
- [ ] Git 커밋 완료 (현재 작업 저장)
- [ ] Visual Studio 2022 실행
- [ ] Dx11_Naruto.sln 열기
- [ ] 솔루션 빌드 성공 확인 (현재 상태)

---

## Phase 1: Client 프로젝트를 StaticLibrary로 변환

### Step 1-1: Game 폴더 생성

**위치:** 솔루션 루트
```
D:\GitDesktop\Dx11_Naruto\.claude\worktrees\hardcore-franklin\
```

**실행:**
1. 탐색기에서 솔루션 루트 폴더 열기
2. 새 폴더 생성: `Game`
3. Game 폴더 내부에 3개 하위 폴더 생성:
   - `Public`
   - `Private`
   - `Default`

**결과:**
```
솔루션 루트/
├── Client/
├── EditorApp/
├── EditorTool/
├── Engine/
└── Game/           ← 새로 생성
    ├── Public/
    ├── Private/
    └── Default/
```

---

### Step 1-2: Client 파일을 Game으로 복사

**복사할 파일 목록:**

#### Default 폴더 (4개 파일)
| 원본 | 대상 |
|------|------|
| `Client/Default/Client.cpp` | `Game/Default/Game.cpp` |
| `Client/Default/Client.h` | `Game/Default/Game.h` |
| `Client/Default/pch.h` | `Game/Default/pch.h` |
| `Client/Default/pch.cpp` | `Game/Default/pch.cpp` |

**실행:**
1. 탐색기에서 `Client/Default/Client.cpp` 선택
2. 복사 (Ctrl+C)
3. `Game/Default/` 폴더에 붙여넣기 (Ctrl+V)
4. 이름 변경: `Client.cpp` → `Game.cpp`
5. 같은 방식으로 나머지 파일도 복사 및 이름 변경

#### Public 폴더 (8개 파일)
| 원본 | 대상 |
|------|------|
| `Client/Public/MainApp.h` | `Game/Public/MainApp.h` |
| `Client/Public/NetworkManager.h` | `Game/Public/NetworkManager.h` |
| `Client/Public/ServerSession.h` | `Game/Public/ServerSession.h` |
| `Client/Public/Client_PacketHandler.h` | `Game/Public/Client_PacketHandler.h` |
| `Client/Public/Client_Defines.h` | `Game/Public/Client_Defines.h` |
| `Client/Public/Client_Enum.h` | `Game/Public/Client_Enum.h` |
| `Client/Public/Client_Macro.h` | `Game/Public/Client_Macro.h` |
| `Client/Public/Protocol_Wrapper.h` | `Game/Public/Protocol_Wrapper.h` |

**실행:** 위와 동일한 방식으로 복사

#### Private 폴더 (4개 파일)
| 원본 | 대상 |
|------|------|
| `Client/Private/MainApp.cpp` | `Game/Private/MainApp.cpp` |
| `Client/Private/NetworkManager.cpp` | `Game/Private/NetworkManager.cpp` |
| `Client/Private/ServerSession.cpp` | `Game/Private/ServerSession.cpp` |
| `Client/Private/Client_PacketHandler.cpp` | `Game/Private/Client_PacketHandler.cpp` |

**실행:** 위와 동일한 방식으로 복사

**완료 후 Game 폴더 구조:**
```
Game/
├── Public/ (8개 파일)
│   ├── MainApp.h
│   ├── NetworkManager.h
│   ├── ServerSession.h
│   ├── Client_PacketHandler.h
│   ├── Client_Defines.h
│   ├── Client_Enum.h
│   ├── Client_Macro.h
│   └── Protocol_Wrapper.h
├── Private/ (4개 파일)
│   ├── MainApp.cpp
│   ├── NetworkManager.cpp
│   ├── ServerSession.cpp
│   └── Client_PacketHandler.cpp
└── Default/ (4개 파일)
    ├── Game.cpp (Client.cpp에서 복사)
    ├── Game.h (Client.h에서 복사)
    ├── pch.h
    └── pch.cpp
```

---

### Step 1-3: Game.vcxproj 프로젝트 파일 생성

**방법 1: Visual Studio에서 생성 (권장)**

1. Visual Studio에서 솔루션 열기
2. 솔루션 탐색기에서 솔루션 우클릭
3. `추가` → `새 프로젝트`
4. 템플릿 선택: `Windows 데스크톱 응용 프로그램` (C++)
5. 프로젝트 이름: `Game`
6. 위치: 솔루션 루트 경로 선택
7. `만들기` 클릭

**방법 2: 수동으로 vcxproj 생성**

`Game/Default/Game.vcxproj` 파일을 생성하고 아래 내용 붙여넣기:

```xml
<?xml version="1.0" encoding="utf-8"?>
<Project DefaultTargets="Build" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <ItemGroup Label="ProjectConfigurations">
    <ProjectConfiguration Include="Debug|x64">
      <Configuration>Debug</Configuration>
      <Platform>x64</Platform>
    </ProjectConfiguration>
    <ProjectConfiguration Include="Release|x64">
      <Configuration>Release</Configuration>
      <Platform>x64</Platform>
    </ProjectConfiguration>
  </ItemGroup>
  <PropertyGroup Label="Globals">
    <VCProjectVersion>17.0</VCProjectVersion>
    <ProjectGuid>{새로운GUID생성}</ProjectGuid>
    <Keyword>Win32Proj</Keyword>
    <RootNamespace>Game</RootNamespace>
    <WindowsTargetPlatformVersion>10.0</WindowsTargetPlatformVersion>
  </PropertyGroup>
  <Import Project="$(VCTargetsPath)\Microsoft.Cpp.Default.props" />
  <PropertyGroup Condition="'$(Configuration)|$(Platform)'=='Debug|x64'" Label="Configuration">
    <ConfigurationType>Application</ConfigurationType>
    <UseDebugLibraries>true</UseDebugLibraries>
    <PlatformToolset>v143</PlatformToolset>
    <CharacterSet>Unicode</CharacterSet>
  </PropertyGroup>
  <PropertyGroup Condition="'$(Configuration)|$(Platform)'=='Release|x64'" Label="Configuration">
    <ConfigurationType>Application</ConfigurationType>
    <UseDebugLibraries>false</UseDebugLibraries>
    <PlatformToolset>v143</PlatformToolset>
    <WholeProgramOptimization>true</WholeProgramOptimization>
    <CharacterSet>Unicode</CharacterSet>
  </PropertyGroup>
  <Import Project="$(VCTargetsPath)\Microsoft.Cpp.props" />
  <ImportGroup Label="PropertySheets" Condition="'$(Configuration)|$(Platform)'=='Debug|x64'">
    <Import Project="$(UserRootDir)\Microsoft.Cpp.$(Platform).user.props" Condition="exists('$(UserRootDir)\Microsoft.Cpp.$(Platform).user.props')" Label="LocalAppDataPlatform" />
  </ImportGroup>
  <ImportGroup Label="PropertySheets" Condition="'$(Configuration)|$(Platform)'=='Release|x64'">
    <Import Project="$(UserRootDir)\Microsoft.Cpp.$(Platform).user.props" Condition="exists('$(UserRootDir)\Microsoft.Cpp.$(Platform).user.props')" Label="LocalAppDataPlatform" />
  </ImportGroup>
  <PropertyGroup Label="UserMacros" />

  <!-- Debug 설정 -->
  <PropertyGroup Condition="'$(Configuration)|$(Platform)'=='Debug|x64'">
    <LinkIncremental>true</LinkIncremental>
    <OutDir>../../Bin/</OutDir>
    <IntDir>../Temp/$(Configuration)/</IntDir>
    <TargetName>Game</TargetName>
  </PropertyGroup>

  <!-- Release 설정 -->
  <PropertyGroup Condition="'$(Configuration)|$(Platform)'=='Release|x64'">
    <LinkIncremental>false</LinkIncremental>
    <OutDir>../../Bin/</OutDir>
    <IntDir>../Temp/$(Configuration)/</IntDir>
    <TargetName>Game</TargetName>
  </PropertyGroup>

  <!-- Debug 컴파일 옵션 -->
  <ItemDefinitionGroup Condition="'$(Configuration)|$(Platform)'=='Debug|x64'">
    <ClCompile>
      <WarningLevel>Level3</WarningLevel>
      <SDLCheck>true</SDLCheck>
      <PreprocessorDefinitions>_DEBUG;_WINDOWS;%(PreprocessorDefinitions)</PreprocessorDefinitions>
      <ConformanceMode>true</ConformanceMode>
      <PrecompiledHeader>Use</PrecompiledHeader>
      <PrecompiledHeaderFile>pch.h</PrecompiledHeaderFile>
      <AdditionalIncludeDirectories>../Public/;../../EngineSDK/Include/;../Default/;$(SolutionDir)Client\Public;$(SolutionDir)Client\Default;$(SolutionDir)Server\ServerCore\Public;$(SolutionDir)Server\Protobuf\Bin;%(AdditionalIncludeDirectories)</AdditionalIncludeDirectories>
      <LanguageStandard>stdcpp20</LanguageStandard>
    </ClCompile>
    <Link>
      <SubSystem>Windows</SubSystem>
      <GenerateDebugInformation>true</GenerateDebugInformation>
      <AdditionalLibraryDirectories>../../EngineSDK/Lib/;../Bin/;%(AdditionalLibraryDirectories)</AdditionalLibraryDirectories>
      <AdditionalDependencies>libprotobuf.lib;Engine.lib;Client.lib;ServerCore.lib;%(AdditionalDependencies)</AdditionalDependencies>
    </Link>
  </ItemDefinitionGroup>

  <!-- Release 컴파일 옵션 -->
  <ItemDefinitionGroup Condition="'$(Configuration)|$(Platform)'=='Release|x64'">
    <ClCompile>
      <WarningLevel>Level3</WarningLevel>
      <FunctionLevelLinking>true</FunctionLevelLinking>
      <IntrinsicFunctions>true</IntrinsicFunctions>
      <SDLCheck>true</SDLCheck>
      <PreprocessorDefinitions>NDEBUG;_WINDOWS;%(PreprocessorDefinitions)</PreprocessorDefinitions>
      <ConformanceMode>true</ConformanceMode>
      <PrecompiledHeader>Use</PrecompiledHeader>
      <PrecompiledHeaderFile>pch.h</PrecompiledHeaderFile>
      <AdditionalIncludeDirectories>../Public/;../../EngineSDK/Include/;../Default/;$(SolutionDir)Client\Public;$(SolutionDir)Client\Default;$(SolutionDir)Server\ServerCore\Public;$(SolutionDir)Server\Protobuf\Bin;%(AdditionalIncludeDirectories)</AdditionalIncludeDirectories>
      <LanguageStandard>stdcpp20</LanguageStandard>
    </ClCompile>
    <Link>
      <SubSystem>Windows</SubSystem>
      <EnableCOMDATFolding>true</EnableCOMDATFolding>
      <OptimizeReferences>true</OptimizeReferences>
      <GenerateDebugInformation>true</GenerateDebugInformation>
      <AdditionalLibraryDirectories>../../EngineSDK/Lib/;../Bin/;%(AdditionalLibraryDirectories)</AdditionalLibraryDirectories>
      <AdditionalDependencies>libprotobuf.lib;Engine.lib;Client.lib;ServerCore.lib;%(AdditionalDependencies)</AdditionalDependencies>
    </Link>
  </ItemDefinitionGroup>

  <!-- 소스 파일 -->
  <ItemGroup>
    <ClCompile Include="..\Default\Game.cpp" />
    <ClCompile Include="..\Default\pch.cpp">
      <PrecompiledHeader Condition="'$(Configuration)|$(Platform)'=='Debug|x64'">Create</PrecompiledHeader>
      <PrecompiledHeader Condition="'$(Configuration)|$(Platform)'=='Release|x64'">Create</PrecompiledHeader>
    </ClCompile>
    <ClCompile Include="..\Private\MainApp.cpp" />
    <ClCompile Include="..\Private\NetworkManager.cpp" />
    <ClCompile Include="..\Private\ServerSession.cpp" />
    <ClCompile Include="..\Private\Client_PacketHandler.cpp" />
  </ItemGroup>

  <!-- 헤더 파일 -->
  <ItemGroup>
    <ClInclude Include="..\Default\Game.h" />
    <ClInclude Include="..\Default\pch.h" />
    <ClInclude Include="..\Public\MainApp.h" />
    <ClInclude Include="..\Public\NetworkManager.h" />
    <ClInclude Include="..\Public\ServerSession.h" />
    <ClInclude Include="..\Public\Client_PacketHandler.h" />
    <ClInclude Include="..\Public\Client_Defines.h" />
    <ClInclude Include="..\Public\Client_Enum.h" />
    <ClInclude Include="..\Public\Client_Macro.h" />
    <ClInclude Include="..\Public\Protocol_Wrapper.h" />
  </ItemGroup>

  <Import Project="$(VCTargetsPath)\Microsoft.Cpp.targets" />
</Project>
```

**중요:** `{새로운GUID생성}` 부분은 Visual Studio에서 자동 생성되거나, 온라인 GUID 생성기를 사용하세요.

---

### Step 1-4: Game 프로젝트를 솔루션에 추가

**Visual Studio에서:**

1. 솔루션 탐색기에서 솔루션 우클릭
2. `추가` → `기존 프로젝트`
3. `Game/Default/Game.vcxproj` 선택
4. `열기` 클릭

**확인:**
- 솔루션 탐색기에 `Game` 프로젝트가 나타나야 함

---

### Step 1-5: Game.cpp 파일 수정

**파일:** `Game/Default/Game.cpp`

**변경사항:**

1. **헤더 include 수정**
```cpp
// 기존:
#include "pch.h"
#include "Client.h"

// 변경:
#include "pch.h"
#include "Game.h"
```

2. **리소스 ID 수정** (74번째 줄 근처)
```cpp
// 기존:
LoadStringW(hInstance, IDC_CLIENT, szWindowClass, MAX_LOADSTRING);

// 변경:
LoadStringW(hInstance, IDC_GAME, szWindowClass, MAX_LOADSTRING);
```

3. **아이콘 리소스 수정** (170, 175번째 줄 근처)
```cpp
// 기존:
wcex.hIcon = LoadIcon(hInstance, MAKEINTRESOURCE(IDI_CLIENT));
wcex.lpszMenuName = MAKEINTRESOURCEW(IDC_CLIENT);
wcex.hIconSm = LoadIcon(wcex.hInstance, MAKEINTRESOURCE(IDI_SMALL));

// 변경:
wcex.hIcon = LoadIcon(hInstance, MAKEINTRESOURCE(IDI_GAME));
wcex.lpszMenuName = MAKEINTRESOURCEW(IDC_GAME);
wcex.hIconSm = LoadIcon(wcex.hInstance, MAKEINTRESOURCE(IDI_SMALL));
```

**저장:** Ctrl+S

---

### Step 1-6: Game.h 파일 수정

**파일:** `Game/Default/Game.h`

**변경사항:**

리소스 정의 수정:
```cpp
// 기존:
#define IDC_CLIENT        109

// 변경:
#define IDC_GAME          109
#define IDI_GAME          107
```

**저장:** Ctrl+S

---

### Step 1-7: Client 프로젝트를 StaticLibrary로 변경

**파일:** `Client/Default/Client.vcxproj`

**변경 방법 1: Visual Studio에서**

1. 솔루션 탐색기에서 `Client` 프로젝트 우클릭
2. `속성` 클릭
3. `구성: 모든 구성`, `플랫폼: 모든 플랫폼` 선택
4. `구성 속성` → `일반`
5. `구성 형식` → `정적 라이브러리(.lib)` 선택
6. `확인` 클릭

**변경 방법 2: vcxproj 파일 직접 수정**

`Client/Default/Client.vcxproj` 파일을 텍스트 에디터로 열기:

```xml
<!-- 기존 (4곳 모두 찾아서 수정) -->
<ConfigurationType>Application</ConfigurationType>

<!-- 변경 -->
<ConfigurationType>StaticLibrary</ConfigurationType>
```

**저장 및 재로드:**
- Visual Studio에서 프로젝트 재로드 확인 메시지 → `예` 클릭

---

### Step 1-8: Client 프로젝트에서 진입점 파일 제거

**Visual Studio에서:**

1. 솔루션 탐색기에서 `Client` 프로젝트 확장
2. `Default/Client.cpp` 찾기
3. 우클릭 → `프로젝트에서 제외` 클릭 (삭제 아님!)

**또는 vcxproj 수정:**

`Client/Default/Client.vcxproj`에서 아래 줄 찾아서 주석 처리:
```xml
<!-- <ClCompile Include="..\Default\Client.cpp" /> -->
```

---

### Step 1-9: Client 프로젝트에서 네트워크 파일 제거

**제거할 파일 목록:**
- `MainApp.h/cpp`
- `NetworkManager.h/cpp`
- `ServerSession.h/cpp`
- `Client_PacketHandler.h/cpp`

**Visual Studio에서:**

1. 솔루션 탐색기 → `Client` 프로젝트
2. 각 파일 우클릭 → `프로젝트에서 제외`

**vcxproj에서 제거:**

아래 항목들을 찾아서 주석 처리:
```xml
<!--
<ClCompile Include="..\Private\MainApp.cpp" />
<ClCompile Include="..\Private\NetworkManager.cpp" />
<ClCompile Include="..\Private\ServerSession.cpp" />
<ClCompile Include="..\Private\Client_PacketHandler.cpp" />

<ClInclude Include="..\Public\MainApp.h" />
<ClInclude Include="..\Public\NetworkManager.h" />
<ClInclude Include="..\Public\ServerSession.h" />
<ClInclude Include="..\Public\Client_PacketHandler.h" />
-->
```

---

### Step 1-10: Client 프로젝트 빌드 설정 수정

**파일:** `Client/Default/Client.vcxproj`

**AdditionalIncludeDirectories에서 제거:**

```xml
<!-- Debug|x64와 Release|x64 모두에서 아래 경로 제거 -->
$(SolutionDir)Server\ServerCore\Public;
$(SolutionDir)Server\Protobuf\Bin;
```

**AdditionalDependencies에서 제거:**

```xml
<!-- Debug|x64와 Release|x64 모두에서 아래 라이브러리 제거 -->
libprotobuf.lib;
ServerCore.lib;
```

**최종 AdditionalDependencies:**
```xml
<AdditionalDependencies>Engine.lib;%(AdditionalDependencies)</AdditionalDependencies>
```

---

## Phase 2: EditorApp를 Editor로 이름 변경

### Step 2-1: EditorApp 폴더 이름 변경

**주의:** Visual Studio를 먼저 닫아야 합니다!

1. Visual Studio 종료
2. 탐색기에서 `EditorApp` 폴더 우클릭
3. `이름 바꾸기` → `Editor`로 변경
4. Enter

---

### Step 2-2: Editor.vcxproj 파일 이름 변경

**위치:** `Editor/Default/`

1. `EditorApp.vcxproj` → `Editor.vcxproj`
2. `EditorApp.vcxproj.filters` → `Editor.vcxproj.filters` (있다면)
3. `EditorApp.vcxproj.user` → `Editor.vcxproj.user` (있다면)

---

### Step 2-3: Editor.vcxproj 내부 수정

**파일:** `Editor/Default/Editor.vcxproj`

텍스트 에디터로 열어서 수정:

1. **프로젝트 이름 변경:**
```xml
<!-- 기존 -->
<RootNamespace>EditorApp</RootNamespace>

<!-- 변경 -->
<RootNamespace>Editor</RootNamespace>
```

2. **출력 파일 이름 변경:**
```xml
<!-- 기존 -->
<TargetName>EditorApp</TargetName>

<!-- 변경 -->
<TargetName>Editor</TargetName>
```

3. **Client.lib 의존성 추가:**
```xml
<!-- AdditionalDependencies에 Client.lib 추가 -->
<AdditionalDependencies>Engine.lib;EditorTool.lib;Client.lib;%(AdditionalDependencies)</AdditionalDependencies>
```

4. **Client include 경로 추가 (이미 있으면 skip):**
```xml
<AdditionalIncludeDirectories>
  ../Public/;
  ../../EngineSDK/Include/;
  ../Default/;
  $(SolutionDir)EditorTool\Public;
  $(SolutionDir)Client\Public;
  $(SolutionDir)Client\Default;
  %(AdditionalIncludeDirectories)
</AdditionalIncludeDirectories>
```

5. **ServerCore 경로 제거:**
```xml
<!-- 아래 경로 삭제 -->
$(SolutionDir)Server\ServerCore\Public;
$(SolutionDir)Server\Protobuf\Bin;
```

---

### Step 2-4: EditorApp_Defines.h → Editor_Defines.h 변경 (선택사항)

**파일:** `Editor/Public/EditorApp_Defines.h`

이름 변경하려면:
1. 파일명 변경: `Editor_Defines.h`
2. 파일 내부 include guard 수정:
```cpp
// 기존:
#ifndef EDITORAPP_DEFINES_H
#define EDITORAPP_DEFINES_H

// 변경:
#ifndef EDITOR_DEFINES_H
#define EDITOR_DEFINES_H
```

3. 이 파일을 include하는 모든 곳 수정:
```cpp
// 기존:
#include "EditorApp_Defines.h"

// 변경:
#include "Editor_Defines.h"
```

---

## Phase 3: 솔루션 파일 업데이트

### Step 3-1: Dx11_Naruto.sln 수정

**파일:** `Dx11_Naruto.sln`

텍스트 에디터로 열어서:

1. **EditorApp → Editor 변경:**
```
# 찾기: "EditorApp"
# 바꾸기: "Editor"
# 모두 바꾸기
```

2. **Game 프로젝트 추가:**

```
Project("{8BC9CEB8-8B4A-11D0-8D11-00A0C91BC942}") = "Game", "Game\Default\Game.vcxproj", "{GUID}"
EndProject
```

**GUID는 Game.vcxproj의 ProjectGuid와 동일하게!**

3. **빌드 구성 추가 (Game):**
```
{GAME-GUID}.Debug|x64.ActiveCfg = Debug|x64
{GAME-GUID}.Debug|x64.Build.0 = Debug|x64
{GAME-GUID}.Release|x64.ActiveCfg = Release|x64
{GAME-GUID}.Release|x64.Build.0 = Release|x64
```

---

### Step 3-2: 프로젝트 의존성 설정

**Visual Studio에서:**

1. 솔루션 다시 열기
2. `Editor` 프로젝트 우클릭 → `빌드 종속성` → `프로젝트 종속성`
3. 체크:
   - ✅ Engine
   - ✅ EditorTool
   - ✅ Client
4. `확인`

5. `Game` 프로젝트 우클릭 → `빌드 종속성` → `프로젝트 종속성`
6. 체크:
   - ✅ Engine
   - ✅ Client
7. `확인`

---

## Phase 4: Client_Defines.h 정리

### Step 4-1: Client_Defines.h에서 NetworkManager include 제거

**파일:** `Client/Public/Client_Defines.h`

```cpp
// 기존:
#include "NetworkManager.h"

// 제거 (주석 처리 또는 삭제)
```

**저장:** Ctrl+S

---

### Step 4-2: Game/Public/Client_Defines.h는 유지

**파일:** `Game/Public/Client_Defines.h`

NetworkManager include는 유지:
```cpp
#include "NetworkManager.h"  // Game.exe에서만 사용
```

---

## Phase 5: 빌드 테스트

### Step 5-1: Clean Solution

**Visual Studio에서:**
1. 메뉴 → `빌드` → `솔루션 정리`
2. 완료 대기

---

### Step 5-2: Rebuild All

1. 메뉴 → `빌드` → `솔루션 다시 빌드`
2. 출력 창에서 빌드 순서 확인:
   - Engine.dll
   - Client.lib
   - EditorTool.lib
   - Editor.exe
   - Game.exe

**예상 결과:**
```
빌드: 성공 5, 실패 0, 최신 0, 건너뛰기 0
```

---

### Step 5-3: 에러 처리

**링크 에러 발생 시:**

#### Error: "NetworkManager를 찾을 수 없음" (Client.lib)
→ Client 프로젝트에서 NetworkManager 참조가 남아있음
→ `Client_Defines.h`에서 `#include "NetworkManager.h"` 제거 확인

#### Error: "Client.lib를 찾을 수 없음" (Editor/Game)
→ 빌드 순서 문제
→ `프로젝트 종속성`에서 Client가 Engine 이후에 빌드되는지 확인

#### Error: "LNK2001 외부 기호를 확인할 수 없습니다"
→ 라이브러리 링크 누락
→ AdditionalDependencies 다시 확인

---

## Phase 6: 런타임 검증

### Step 6-1: Editor.exe 실행

1. 솔루션 탐색기에서 `Editor` 프로젝트 우클릭
2. `시작 프로젝트로 설정`
3. F5 (디버그 시작)

**확인사항:**
- [ ] 에디터 윈도우 정상 실행
- [ ] Scene View, Game View 렌더링
- [ ] Hierarchy, Inspector 작동
- [ ] Play 버튼 클릭 시 게임 씬 동작

---

### Step 6-2: Game.exe 실행

1. 솔루션 탐색기에서 `Game` 프로젝트 우클릭
2. `시작 프로젝트로 설정`
3. F5 (디버그 시작)

**확인사항:**
- [ ] 게임 윈도우 정상 실행
- [ ] NetworkManager 초기화 성공 (콘솔 로그 확인)
- [ ] 게임 씬 로드 성공
- [ ] 게임 루프 동작

---

### Step 6-3: 멀티플레이 테스트 (Editor에서)

1. Editor.exe 실행
2. 메뉴 또는 버튼에서 "Start Multiplayer (2 Players)" 선택

**확인사항:**
- [ ] GameServer.exe 프로세스 실행 확인 (작업 관리자)
- [ ] Game.exe 프로세스 2개 실행 확인
- [ ] 각 윈도우 "Player 1", "Player 2" 타이틀 확인
- [ ] 윈도우 좌우 배치 확인

---

## Phase 7: 최종 정리

### Step 7-1: 불필요한 파일 정리

**탐색기에서 삭제 (선택사항):**
- `Client/Default/Client.cpp` (Game으로 복사했으므로)
- `Client/Default/Client.h`
- `Client/Private/MainApp.cpp`
- `Client/Private/NetworkManager.cpp`
- `Client/Private/ServerSession.cpp`
- `Client/Private/Client_PacketHandler.cpp`
- `Client/Public/MainApp.h`
- `Client/Public/NetworkManager.h`
- `Client/Public/ServerSession.h`
- `Client/Public/Client_PacketHandler.h`

**주의:** 삭제 전 Git 커밋 권장!

---

### Step 7-2: Git 커밋

```bash
git add .
git commit -m "refactor: Client(lib), Editor(exe), Game(exe) 구조로 리팩토링

- Client를 StaticLibrary로 변환
- Game.exe 프로젝트 생성 (네트워크 포함)
- EditorApp → Editor 이름 변경
- 의존성 정리: Editor/Game 모두 Client.lib 사용
- EditorTool.lib는 유지"
```

---

## ✅ 완료 체크리스트

- [ ] Client.vcxproj → StaticLibrary 변경 완료
- [ ] Game 프로젝트 생성 및 파일 복사 완료
- [ ] EditorApp → Editor 이름 변경 완료
- [ ] 솔루션 파일 업데이트 완료
- [ ] 프로젝트 의존성 설정 완료
- [ ] 빌드 성공 (5개 프로젝트 모두)
- [ ] Editor.exe 실행 성공
- [ ] Game.exe 실행 성공
- [ ] 멀티플레이 테스트 성공
- [ ] Git 커밋 완료

---

## 🚨 문제 발생 시 복구 방법

### 빌드 실패 시
1. Clean Solution
2. 각 프로젝트 vcxproj 파일 다시 확인
3. 프로젝트 종속성 다시 설정
4. Visual Studio 재시작

### 실행 오류 시
1. Bin 폴더에 모든 dll 파일 있는지 확인
2. Engine.dll, Client.lib 빌드 완료 확인
3. 작업 디렉토리가 Bin 폴더로 설정되어 있는지 확인

### 되돌리기
```bash
git reset --hard HEAD
git clean -fd
```

---

## 📁 최종 프로젝트 구조

```
Dx11_Naruto/
├── Engine/           (DynamicLibrary)
│   └── Engine.dll
├── Client/           (StaticLibrary) ← 변경됨
│   ├── Public/       (Level, GameObject, Component)
│   ├── Private/
│   └── Default/
│       └── Client.lib
├── EditorTool/       (StaticLibrary)
│   └── EditorTool.lib
├── Editor/           (Application) ← EditorApp에서 변경
│   └── Default/
│       └── Editor.exe
└── Game/             (Application) ← 신규
    ├── Public/       (NetworkManager, MainApp)
    ├── Private/
    └── Default/
        └── Game.exe
```

---

**리팩토링 완료! 🎉**
