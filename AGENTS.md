# Dx11_Naruto Agent Rules

## 응답 방식 선호 규칙

- 사용자가 기존 코드와 새 코드의 차이를 물으면, 먼저 "무엇이 다른지"를 1-3줄로 바로 말한다.
- 장황한 전체 코드 재제시보다 핵심 변경점, 위치, 이유 순서로 답한다.
- 사용자가 "지금이랑 뭐가 다른지", "차이만", "핵심만"처럼 말하면 전체 설명을 줄이고 변경된 조건/분기/변수만 짚는다.
- 코드 수정 가이드에서는 필요한 경우 전체 코드를 주되, 먼저 변경된 줄이나 조건을 요약한다.

이 레포에서 작업할 때 아래 규칙을 우선 적용한다.

## 기본 기준

- 작업 시작 시 [PROJECT_STRUCTURE.md](/d:/GitDesktop/Dx11_Naruto/PROJECT_STRUCTURE.md)를 먼저 읽고 현재 구조를 기준으로 판단한다.
- 구조나 파일 위치를 설명할 때는 현재 레포 기준 실제 파일/폴더를 우선한다.
- 문서, 플랜, 리뷰, 설계서 작업은 요약 치환보다 보존 + 보강 방식을 우선한다.

## 문서 보강 규칙

- 기존 본문, 코드, 설명은 삭제하지 않는다.
- 최신안은 바로 아래에 `[대체]`, `[변경]`, `[추가]` 태그와 함께 다시 적는다.
- 변경된 파일은 무엇을 어떻게 수정하는지 코드와 함께 보여줘야 한다.
- 문서가 초안에서 상세판으로 확장될 때는 요약 대체가 아니라 보존 + 보강 방식으로 작성한다.
- 코드 예시는 축약하지 말고, 구현에 필요한 헤더/CPP 수준까지 가능한 한 완성형으로 제시한다.


## 주석 강제 규칙

- 새로 추가하는 변수, 멤버 변수, 상태 변수에는 역할이 보이도록 주석을 반드시 붙인다.
- 새로 추가하는 함수, static 함수, helper 함수, private 함수에는 왜 필요한지와 언제 호출되는지 주석을 반드시 붙인다.
- 헤더에 선언만 추가할 때도 주석을 붙이고, CPP 본문에도 필요한 설명 주석을 다시 붙인다.
- 사용자가 `코드 포함`, `완성본`, `A to Z`, `자세하게` 같은 표현을 쓴 경우에는 새로 추가되는 변수/함수 주석을 생략하면 안 된다.
- 문서 예시 코드, 설계 문서 코드, 실제 수정 코드 모두 동일하게 적용한다.
- "이 정도는 자명하다"는 이유로 새 심볼 주석을 생략하지 않는다.
- 기존 코드에 주석이 부족하더라도, 최소한 내가 새로 추가하거나 수정한 변수/함수에는 주석을 반드시 보강한다.

## 코드 포함 문서 요청 시 규칙

- 사용자가 `코드 포함`, `A to Z`, `완성본`, `자세하게`, `어디에 뭘 만들어야 하는지` 같은 표현을 쓰면, 파일별 수정 위치와 코드 예시를 함께 제공한다.
- 기존 코드 초안이 있더라도 삭제하지 않고, 최신안 코드를 바로 아래에 추가한다.
- 수정 대상이 헤더와 CPP로 나뉘는 경우 둘 다 제시한다.
- 함수 시그니처만 주지 말고, 가능한 범위에서 본문 구현 코드까지 함께 제시한다.

## 스타일 선호 규칙

- 사용자가 별도 요청하지 않는 한 enum 문자열 처리나 enum 순회는 `magic_enum` 사용을 우선한다.
- 익명 `namespace {}` 방식은 사용자가 선호하지 않으므로 특별한 이유가 없으면 쓰지 않는다.
- 파일 로컬 헬퍼가 필요하면 익명 namespace 대신 `static` 함수 또는 클래스의 `private static` 함수 방식을 우선 검토한다.
- 새 가이드나 예시 코드를 작성할 때도 위 기준을 동일하게 적용한다.
